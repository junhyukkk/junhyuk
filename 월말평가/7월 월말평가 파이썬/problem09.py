############## 주의 ##############
# 입력을 받기위한 input 함수는 절대 사용하지 않습니다.
# 내장 함수 sum, min, max, len 함수를 사용하지 않습니다.
# 사용시 감점처리 되니 반드시 확인 바랍니다.
def sum_digits(number):
    pass
    # 여기에 코드를 작성하여 함수를 완성합니다.
    
    
    
# 아래 코드는 실행 확인을 위한 코드입니다.
if __name__ == '__main__':
    # 예시 코드는 수정하지 마세요.
    print(sum_digits(123))  # => 6
    # 여기부터 아래에 추가 테스트를 위한 코드 작성 가능합니다.



def sum_digits(number):
    total = 0
    while number > 0:
        total += number % 10  # number를 10으로 나눈 나머지가 해당 자릿수
        number //= 10  # number를 10으로 나눈 몫을 다시 number로 갱신
    return total

# 아래 코드는 실행 확인을 위한 코드입니다.
if __name__ == '__main__':
    print(sum_digits(123))  # => 6
'''주어진 문제는 주어진 숫자 number의 각 자릿수를 추출하여 
이들을 모두 더하는 함수 sum_digits를 구현하는 것입니다.

함수를 작성하기 위해서는 다음과 같은 과정을 수행해야 합니다:

number를 10으로 나누어가면서 각 자릿수를 추출합니다.
추출한 자릿수를 누적하여 더합니다.
예를 들어, number가 123인 경우, 1, 2, 3을 추출하여 
이들을 더하면 최종 결과로 6이 출력됩니다.
 코드에서 number % 10 연산을 통해 number를 10으로 나눈 나머지를 구하면 해당 자릿수가 추출됩니다. 
 추출한 자릿수는 total에 누적하여 더해지고, number는 number //= 10 연산을 통해 10으로 나눈 몫으로 갱신됩니다. 이렇게 하면 자릿수를 모두 추출할 때까지 반복하게 되고, 
 결과로 6이 출력
'''